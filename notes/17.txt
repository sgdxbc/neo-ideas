id
17
create
2024-06-18T12:54:18.997524483+08:00
parent
2

目前的存储结构是一组相当扁平的人类可读文件。文件系统所包含的名字和关系并没有编码任何信息。构建程序只是简单地以任意顺序读取文件夹下的所有文件内容，然后进行索引，重建所有的交叉引用关系。最后根据指定的命令决定是渲染整个站点，还是给某一张卡片增加一条更新时间戳，还是什么别的。

目前构建程序会每执行一次命令就重建整个索引。等到我写了足够多的东西进去以至于命令的运行时间达到3秒时再着手实现缓存索引的功能。