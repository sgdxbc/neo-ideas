id
22
create
2024-06-18T14:02:18.301029360+08:00
parent
2

接下来要做的，首先一个是增加基本的对markdown的支持。但是这个主要只是为了向后兼容，并不准备在新写的内容上大量用markdown。

这个想用很简单的方式去随手就做了，调个社区库就行了不用自己从头写。使用场景基本上也局限在最基础的加个粗加个斜体这个样子。非常简单地生成几个html标签，然后我在自己的样式里面再去调它的外观。

毕竟现在的这种纯文本格式其实也是可以<strong>随便</strong><em>加入</em><code>html</code>的。大差不差。